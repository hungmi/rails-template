<!DOCTYPE html>
<html>
<head>
    <title><%%= meta_title %></title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="format-detection" content="telephone=no">
    <meta name="viewport" content="initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, width=device-width">
    <%%#= stylesheet_pack_tag 'admin', media: 'all', 'data-turbo-track': 'reload' %>
    <%%= csrf_meta_tags %>
    <%%= csp_meta_tag %>
    <%%#= favicon_link_tag "favicon.ico" %>
    <%%= javascript_pack_tag 'admin', 'data-turbo-track': 'reload' %>
    <link href="https://fonts.googleapis.com/earlyaccess/notosanstc.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>
<body class='admin <%%= "admin-#{controller_name}-#{action_name}" %>'>
    <%%= render "admin/common/nav_top" if user_signed_in? %>
    <div class="container">
        <%%= notice_message(dismissible: true, fixed: true) %>
        <h1 data-controller="hello" data-target="hello.output"></h1>
        <%%= yield %>
    </div>
</body>
</html>